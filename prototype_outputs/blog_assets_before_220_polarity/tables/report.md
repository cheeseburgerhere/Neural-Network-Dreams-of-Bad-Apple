# Blog tables export

Exact per-frame measurements and copy-ready Markdown summaries. All main scores exclude frames 0–15; polarity diagnostics are explicitly separate. Source checkpoint identities are in source_summaries.json.
